- include: init_symfony_app.yml
  # Passing through variable: dir

- include: supervisorify.yml
  vars:
    service: "{{name}}"
    command: "/usr/bin/php5 ./app/console server:run -vv {{ansible_eth1.ipv4.address}}:80"
    chdir: "{{dir}}"
